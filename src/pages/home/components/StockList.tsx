import React from "react";
import { getPriceColor } from "@/lib/getPriceColor";

interface IProps {
    list: any
}

const StockList: React.FC<IProps> = ({ list }) => {


    return (
        <div className={`flex items-center gap-1 `}>

        </div>
    );
};

export default StockList;
